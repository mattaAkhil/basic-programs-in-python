def rev(n):
    rev=0
    while n!=0:
        r=n%10
        rev=rev*10+r
        n=n//10
    return rev    
    
n=int(input())
re=rev(n)
if n==re:
    print(f"{n} is a palindrome")
else:
    print(f"{n} is not a palindrome")
        
